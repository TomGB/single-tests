{
  "name": "ast-test",
  "version": "1.0.0",
  "description": "",
  "main": "split/split.js",
  "scripts": {
    "test": "./testSplit.sh",
    "split": "node lib/split.js --config split.config.js",
    "test:beforeSplit": "rm -rf split-tmp && jest exampleTests",
    "test:afterSplit": "jest split-tmp --json --outputFile=splitTestResults.json",
    "test:splitOutcome": "jest lib/splitOutcome.test.js"
  },
  "jest": {
    "verbose": true
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "acorn": "^6.1.1",
    "ast-traverse": "^0.1.1",
    "glob-promise": "^3.4.0",
    "globby": "^9.2.0",
    "transform-ast": "^2.4.4"
  },
  "devDependencies": {
    "jest": "^24.6.0"
  }
}
